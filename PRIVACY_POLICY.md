# 薯页摘录隐私政策

最后更新：2026 年 9 月 18 日

## 1. 适用范围与单一用途

薯页摘录是一款本地运行的 Chrome/Edge 扩展。它的单一用途是：在用户主动点击扩展并确认处理当前小红书或微博帖文详情页后，提取该页面上用于概括的正文、图片、视频字幕与画面帧、互动数据与评论，并调用用户自行配置的文字和图片模型生成中文舆情简报。用户还可以把最多 5 条帖文（含由用户上传的已删除帖文截图识别而成的证据）加入合并清单并生成合并简报，复制或下载结果，并可选择把最终简报发送到飞书。

扩展不会在后台持续监控浏览活动，也不会读取与该次用户操作无关的页面。

## 2. 扩展处理的数据

根据用户主动使用的功能，扩展可能处理以下数据：

- 当前小红书或微博帖文的完整 URL、帖文 ID、标题（微博无独立标题）、作者或用户名、正文、话题、页面日期或发布时间、页面显示的地点或属地、点赞数、收藏数、转发数（微博）和评论数；
- 帖文图片及其中可见的文字和视觉信息；
- 视频类帖文的平台自动语音字幕文本（SRT，仅小红书）与按时间点截取的视频画面帧（微博为播放器当前帧）：字幕文本与画面帧（压缩后的 JPEG）会发送到用户配置的模型 API 作为概括证据，字幕文本与画面帧的识别结果进入会话缓存，视频文件本身不会被下载或保存；
- 最多 50 条一级评论，以及页面已经显示的回复，包括评论内容、用户名或用户 ID、点赞数、页面日期、页面显示的地点或属地等字段（小红书为当前页面顺序，微博为接口分页返回顺序）；
- 用户主动上传的帖文截图：压缩后发送到用户配置的图片模型 API 识别，识别出的文本（标题、正文、账号、时间、互动数、可见评论）进入会话缓存；截图图片二进制本身不会保存；
- 用户自行填写的模型 API 地址、模型名称和 API Key；
- 用户主动配置飞书功能时填写的 Webhook 地址、签名密钥、应用 App ID、App Secret、接收人企业邮箱或 Open ID；
- 当前小红书页面中名为 `web_session`、微博页面中名为 `SUB` 的 Cookie 是否存在。扩展只用它判断是否已经登录，不保存、显示、记录或传输 Cookie 值；
- 扩展生成的图片识别证据、概括结果、合并清单、任务进度和用户选择的设置。

扩展不会读取 Chrome 浏览历史列表，不会记录用户的键盘输入、鼠标位置、一般点击行为或其他行为遥测。

## 3. 数据用途

上述数据仅用于以下用户可见功能：

- 从用户当前选择的小红书或微博帖文生成结构化中文简报；
- 把合并清单中最多 5 条帖文的证据（含截图识别结果）一次性生成合并简报；
- 识别帖文图片或用户上传截图中的文字和有简报价值的视觉信息；
- 复制结果或按用户请求下载 JSON、Markdown、CSV 和帖文图片；
- 保存模型配置、凭据保存模式、任务进度及当前会话结果；
- 在用户主动启用飞书功能后发送最终简报或测试消息；
- 在任务开始前确认小红书或微博登录状态，避免未登录时启动采集。

这些数据不会用于广告、用户画像、信用评估、贷款决策、出售数据或与上述单一用途无关的目的。

## 4. 数据传输和第三方服务

扩展没有开发者运营的中转服务器。数据只会在实现用户所选功能所必需时，直接从扩展传输到以下由用户选择或启用的服务：

### 4.1 用户配置的图片模型 API

帖文图片和用户上传的帖文截图会由扩展读取（截图先压缩）并临时转换为 Base64 数据，随识别提示词发送到用户配置的 HTTPS 图片模型接口。接口返回的文本或 JSON 只作为数据解析，不会作为代码执行。

### 4.2 用户配置的文字模型 API

为生成简报，扩展会向用户配置的 HTTPS 文字模型接口发送当前帖文 URL 和 ID、标题、作者、日期、正文、话题、评论正文及点赞等必要证据，视频帖还包括平台自动字幕文本，以及图片模型返回的有效识别结果；合并概括时发送清单内全部帖文的上述证据。发送给文字模型的评论证据不包含评论者用户名、用户 ID 或页面地点字段。

### 4.3 飞书（可选）

飞书推送默认关闭。只有用户主动配置并启用后，扩展才会把最终简报直接发送到用户指定的飞书群机器人或账号。自建应用模式还会向飞书开放平台发送用户配置的 App ID、App Secret 和接收人企业邮箱或 Open ID，以获取访问令牌并发送消息。

第三方模型服务和飞书对收到数据的处理受其各自的服务条款和隐私政策约束。用户应只配置自己信任且有权使用的服务。

## 5. 本机存储与保留期限

- 非敏感模型配置和用户偏好保存在 `chrome.storage.local`，直至用户修改、清除扩展数据或卸载扩展；
- API Key、飞书 Webhook、签名密钥和 App Secret 可根据用户选择保存在 `chrome.storage.local`，或仅保存在 `chrome.storage.session`；会话模式中的数据会在浏览器关闭、扩展重新加载或更新后清除；
- 当前采集数据、图片识别证据、概括结果、合并清单（含截图识别出的文本证据）、缓存和任务状态保存在有数量限制的 `chrome.storage.session` 会话缓存中；
- 用户主动下载的 JSON、Markdown、CSV 和图片文件会保留在用户设备的下载目录，直至用户自行删除；
- 扩展不会把上述数据同步到开发者服务器。

本机持久存储侧重个人设备上的使用便利，不是独立加密保险箱。扩展限制内容脚本读取敏感凭据并提供一键清除功能，但对保存在 Chrome 配置文件中的值不再额外加密。请勿在多人共用或不受信任的浏览器配置中长期保存敏感凭据；可以选择会话存储模式。

## 6. 数据共享与出售

开发者不出售用户数据，也不会把用户数据转移给第三方用于广告、营销、画像、信用评估或与扩展单一用途无关的活动。只有在提供用户主动选择的模型处理或飞书推送功能所必需时，数据才会传输到相应服务。

## 7. 用户控制

用户可以：

- 不点击“提取并概括”，从而不启动页面数据处理；
- 选择自己的模型服务和 API 地址，并在 Chrome 权限提示中拒绝相应域名访问；
- 关闭飞书推送；
- 选择持久或会话凭据保存模式；
- 在设置页清除全部模型 API Key 和飞书凭据；
- 清除扩展存储、删除已下载文件或卸载扩展以移除本机数据。

## 8. 安全措施

扩展只允许 HTTPS 模型接口，并通过 HTTPS 与飞书开放平台通信。模型 API Key 和飞书凭据只由扩展的可信页面和后台程序读取，不会注入小红书或微博页面。Cookie 值不会存储、显示、导出或传输。

## 9. Chrome Web Store Limited Use 声明

The use of information received from Google APIs will adhere to the Chrome Web Store User Data Policy, including the Limited Use requirements.

扩展对用户数据的使用仅限于提供或改进已披露的单一用途，不会用于个性化广告，也不会允许开发者人员读取用户数据，法律要求、安全调查或用户就特定支持请求作出明确授权的情形除外。

## 10. 政策变更

如果扩展的数据处理方式发生重大变化，本政策将同步更新，并在适用时更新 Chrome Web Store 的数据披露。更新后的日期会显示在本页顶部。

## 11. 联系方式

如有隐私问题或数据处理疑问，请通过项目公开问题页面联系开发者：

https://github.com/monika2333/xiaohongshu_plugin/issues
